[build]
builder = "nixpacks"
buildCommand = "cd strategy-engine && npm install && npm run build"

[deploy]
startCommand = "cd strategy-engine && node dist/index.js"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
