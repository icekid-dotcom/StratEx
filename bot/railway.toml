[build]
builder = "nixpacks"
buildCommand = "cd bot && npm install && npm run build"

[deploy]
startCommand = "cd bot && node dist/index.js"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
